import { CategoryFilterIdPipe } from './category-filter-id.pipe';

describe('CategoryFilterIdPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryFilterIdPipe();
    expect(pipe).toBeTruthy();
  });
});
