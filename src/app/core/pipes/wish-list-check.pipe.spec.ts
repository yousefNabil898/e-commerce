import { WishListCheckPipe } from './wish-list-check.pipe';

describe('WishListCheckPipe', () => {
  it('create an instance', () => {
    const pipe = new WishListCheckPipe();
    expect(pipe).toBeTruthy();
  });
});
